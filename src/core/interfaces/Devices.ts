export interface Devices {
  id: string;
  name: string;
  description: string;
  type: string;
  locationId: string;
  enabled: boolean;
  priceMode: string;
  autoClose: boolean;
  createdAt: string;
  updatedAt: string;
}
